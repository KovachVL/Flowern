# Path to the Docker daemon socket on the host, mounted into the `server`
# container so it can `docker exec` into the `joern` container.
#
# Default (Docker Desktop / native Linux docker): /var/run/docker.sock
# Colima:  ~/.colima/default/docker.sock  (run `colima status` to confirm)
DOCKER_SOCK=/var/run/docker.sock
